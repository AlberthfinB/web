export interface IEventComing {
    name_event: string;
    image_event: string;
    event_id: number;
    description: string;
    location: string;
  }
  
export interface ICategoryEvent {
    event_category_id: number;
    event_category_name: string;
}